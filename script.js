function login(provider) {
  alert('Logging in with ' + provider + '...');
  // Implement login logic here
}
function guestLogin() {
  alert('Guest login started. Valid for 1 day.');
  // Save session expiration in localStorage
}